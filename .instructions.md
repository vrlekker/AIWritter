# Workspace Instructions: AIWritter

- Prefer implementation patterns that keep all compute and hosting on GitHub.
- Treat repository JSON files as the persistent data store.
- Place automation logic in `engine/` as .NET 8 console code.
- Keep the frontend static and GitHub Pages compatible under `src/MainSite`.
- If adding new workflows, ensure they use `GITHUB_TOKEN` and least-privilege permissions.
